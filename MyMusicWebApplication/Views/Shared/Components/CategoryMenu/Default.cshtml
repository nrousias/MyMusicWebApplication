@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="Home" asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Music Albums<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="MusicAlbum" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="MusicAlbum" asp-action="List" asp-route-Category="">View all music albums</a>
        </li>
    </ul>
</li>